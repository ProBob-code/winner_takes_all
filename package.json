{
  "name": "winner-takes-all",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,frontend -c blue,green \"pnpm dev:api\" \"pnpm dev:frontend\"",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build",
    "build:cloudflare": "pnpm build && SKIP_WRANGLER_CONFIG_CHECK=true npx @opennextjs/cloudflare build --dangerouslyUseUnsupportedNextVersion && mv apps/frontend/.open-next/worker.js apps/frontend/.open-next/_worker.js && cp -r apps/frontend/.open-next/assets/* apps/frontend/.open-next/ && cp apps/frontend/_routes.json apps/frontend/.open-next/_routes.json",
    "build:frontend": "pnpm --filter frontend build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter api test"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.19.4",
    "@types/node": "^22.19.17",
    "concurrently": "^9.1.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
